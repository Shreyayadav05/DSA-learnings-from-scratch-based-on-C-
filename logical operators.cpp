#include <bits/stdc++.h>
using namespace std;

int main() {
    bool x = true;
    bool y = false;
    
    cout << (x && y) << endl; // Output: 0 (false)
    cout << (x || y) << endl; // Output: 1 (true)
    cout << (!x) << endl;     // Output: 0 (false)
    
    return 0;
}
