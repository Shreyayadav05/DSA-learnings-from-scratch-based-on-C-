#include <bits/stdc++.h>
using namespace std;

int main() {
    int runs;
    cin >> runs; // Input: 90
    if (runs >= 100) {
        cout << "Century";
    } 
    else {
        cout << "Not a Century"; // Output: Not a Century
    }
    return 0;
}
